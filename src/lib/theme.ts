/**
 * The site's palette for places that cannot read CSS: the Open Graph card and
 * the browser chrome colour. Mirrors the `:root` tokens in globals.css, which
 * stay the source of truth for the page itself.
 */
export const PALETTE = {
  bg: "#09090a",
  ink: "#f3f3ee",
  ink2: "#a9a9a2",
  ink3: "#74746d",
  rule: "#222225",
  accent: "#c6ff2e",
} as const;
